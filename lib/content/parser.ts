// Simplified markdown parser - will be enhanced with proper MDX rendering later
export async function markdownToHtml(markdown: string): Promise<string> {
  // For now, return the markdown as-is
  // In production, this will use a proper markdown renderer
  return markdown
}

